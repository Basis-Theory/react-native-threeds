# Bridge POC example

This executable example compares the existing WebView renderer with a
handwritten React Native Bridge adapter on iOS and Android.

- Expo 51
- React Native 0.74.5
- New Architecture disabled
- Runtime choices: WebView or Bridge

See `../../docs/LOCAL-RUNBOOK.md` for setup and execution commands.
